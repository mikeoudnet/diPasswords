namespace diPasswords.Domain.Enums
{
    // Перечисление названий наборов элементов
    public enum ElementMode
    {
        None,
        Authorization,
        Adding,
        Deleting,
        Using,
        DataSelected
    }
}
